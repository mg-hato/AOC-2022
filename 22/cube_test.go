package main

import (
	"testing"
)

func TestD22_CubeExample(t *testing.T) {
	// test_env, err := reading.ReadWith(reader.MonkeyMapReader)("./tests/example.txt")
	// testers.AssertNoError(t, err)
	// tf, ti := test_env.Get().Get()
	// cube_map, err := m.CubeMapCreator(tf)
	// testers.AssertNoError(t, err)
	// ptr := cube_map.GetInitialPointer()

}
